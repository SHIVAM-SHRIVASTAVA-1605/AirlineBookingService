const { BookingSrvice } = require('../services/index');BookingSrvice

const bookingService = new BookingService();

const create = async (req, res) => {
    try {
        const response = await  
    } catch(error) {

    }
}

module.exports = {

}